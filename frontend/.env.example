# Leave unset in local dev: the app uses /api → Vite proxy → backend (same origin as :5173).
# Only set this for special setups (e.g. mobile device hitting your machine’s LAN IP).
# VITE_API_URL="http://localhost:4000/api"
