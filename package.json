{
  "name": "side-gig",
  "private": true,
  "scripts": {
    "deploy:up": "docker compose -f docker-compose.prod.yml --env-file .env up --build -d",
    "deploy:down": "docker compose -f docker-compose.prod.yml down",
    "deploy:logs": "docker compose -f docker-compose.prod.yml logs -f",
    "db:up": "docker compose up -d db",
    "db:down": "docker compose down",
    "db:migrate": "npm --prefix backend exec -- npx prisma migrate deploy",
    "db:seed": "npm --prefix backend exec -- npx prisma db seed",
    "dev:setup": "npm run db:migrate && npm run db:seed",
    "test:backend": "npm --prefix backend run test:all",
    "test:frontend": "npm --prefix frontend run test && npm --prefix frontend run test:e2e",
    "test:all": "npm run test:backend && npm run test:frontend",
    "test:load": "npm --prefix backend run test:load"
  }
}

