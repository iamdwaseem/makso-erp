"use client";

export default function LabelPrintSettingsPage() {
  return (
    <div>
      <h1 className="mb-4 text-xl font-bold text-gray-900">LABEL PRINT</h1>
      <p className="mb-6 text-sm text-gray-500">Create labels for products (production companies). Configure template and print options.</p>
      <div className="rounded-lg border border-gray-200 bg-white p-8 text-center text-gray-500">
        Label templates and print settings. Add template name, size, and fields to print.
      </div>
    </div>
  );
}
