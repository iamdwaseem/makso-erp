"use client";

export default function SettingsNumberSettingsPage() {
  return (
    <div>
      <h1 className="mb-2 text-xl font-bold text-gray-900">NUMBER SETTINGS</h1>
      <p className="text-sm text-gray-600">
        Configure number formats and sequences (e.g. invoice numbering). This section can be extended when needed.
      </p>
    </div>
  );
}
